# Worklog 2026-03-19

## Summary

- Restored live review crawling for review-management workflows.
- Refactored reply generation so it follows the explicitly selected review card.
- Re-verified the end-to-end flow in the browser and updated the agent docs with recurring gotchas.

## Submodule Changes

### `pulse_python`

- Reworked Naver crawling to:
  - resolve the actual place detail page from search results
  - move to `/review/visitor`
  - expand review bodies
  - scroll and parse review cards from DOM
- Kept raw review snapshots versioned in Mongo and bumped snapshot version to `4` so old low-count snapshots refresh automatically.
- Verified live crawl result for `바람난 얼큰 수제비 범계점`:
  - total `100`
  - naver `80`
  - kakao `20`

### `pulse_spring`

- Review-management context now requests refreshed review snapshots from FastAPI.
- Review list returned to frontend is now limited to the latest `up to 30 per source`, then merged by date.
- Existing review-management and auth integration remains aligned to the current frontend contract.

### `pulse_FE`

- Review card selection is now the source of truth for reply generation.
- `빠른 설정` no longer uses the first rendered review by default when a different review is selected.
- Browser verification confirmed:
  - selecting a Kakao review in `리뷰관리`
  - moving to `빠른 설정`
  - generating a reply for that same selected review

## Verification

- `pulse_FE`: `npm run build`
- `pulse_spring`: `gradlew.bat compileJava`
- `pulse_python`: `python -m compileall app`
- Playwright E2E:
  - review-management screen loaded live data
  - latest review list rendered `Naver 30 / Kakao 20`
  - selected review reply generation worked on a non-first Kakao review

## Follow-up Note

- Kakao currently yields `20` reviews on the verified live path for this store.
- The code now supports `up to 30 per source`, but if Kakao must always exceed `20` here, the Kakao collection path should be investigated further in a later task.
