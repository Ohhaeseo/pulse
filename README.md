# PULSE 개발 일지

PULSE는 외식업 자영업자를 위한 AI 마케팅 자동화 서비스입니다.  
리뷰 데이터를 수집하고 분석해 고객 인사이트를 도출하고, 이를 바탕으로 콘텐츠 기획, 리뷰 답변, 홍보 실행까지 이어지는 흐름을 하나의 서비스 안에서 제공하는 것을 목표로 개발하고 있습니다.

## 프로젝트 구성

- `pulse_FE`: React/Vite 기반 프론트엔드
- `pulse_spring`: Spring Boot 기반 메인 백엔드
- `pulse_python`: FastAPI 기반 AI/크롤링 서버
- `business`: 예창패 사업계획서 및 제출 문서 작업물
- `.agents`: 에이전트 가이드, 컨텍스트, 스킬 모음

## 한눈에 보는 현재 상태

- 회원가입부터 분석 실행, 대시보드 진입, 리뷰 관리, AI 답변 생성까지 기본 E2E 흐름이 동작합니다.
- 백엔드는 Spring + MySQL, AI 서버는 FastAPI + MongoDB 조합으로 연결되어 있습니다.
- 프론트는 목업 화면을 넘어서 실제 분석 결과와 리뷰 데이터를 받아 보여주는 단계까지 확장되었습니다.
- 최근에는 크롤링 정확도, 차트 안정성, 실데이터 기반 사용자 표시 같은 완성도 이슈를 집중적으로 다듬었습니다.
- `홍보 영상 만들기`는 이제 `손님 분석`의 실제 페르소나를 받아 타겟 손님으로 사용하고, 페르소나/스타일별 AI 자동 완성 프롬프트를 추천하는 흐름까지 연결되었습니다.
- 루트에서 `scripts/run_full_validation.ps1` 한 번으로 FE lint/build, Spring test, Python pytest, Playwright 스모크 E2E까지 연속 검증할 수 있습니다.

## 날짜별 개발 일지

### 2025-11-24
- Python 기반 AI 서버의 초기 구조를 잡기 시작했습니다.
- 리뷰 수집과 분석을 분리 가능한 서비스 형태로 만들기 위한 첫 기반을 마련했습니다.

### 2025-11-25
- Python 레이어 구조와 실행 흐름을 조금 더 정리하면서 서버 골격을 다듬었습니다.
- 이후 FastAPI 중심 구조로 재편할 수 있는 발판을 만들었습니다.

### 2026-01-26
- Spring Boot 백엔드의 초기 세팅을 정리하고, 기본 API가 동작하는 상태까지 확인했습니다.
- 인증과 사용자 관리 기능을 붙일 수 있는 메인 백엔드 기반이 자리 잡았습니다.

### 2026-01-27
- FastAPI 서버를 레이어드 아키텍처로 리팩터링했습니다.
- Spring 쪽도 인증 DTO와 보안 흐름을 정리해 로그인/인가 안정성을 높였습니다.

### 2026-02-02
- 각 저장소를 하나의 루트에서 관리할 수 있도록 모노레포 구조를 정리했습니다.
- FE, Spring, Python 저장소를 Git 서브모듈 형태로 전환해 병렬 개발과 동기화를 수월하게 만들었습니다.

### 2026-02-05
- 서브모듈 관리 가이드를 추가해 환경 정리와 원격 동기화 절차를 명확히 했습니다.
- Spring에서는 회원가입 DTO와 카테고리 역직렬화 쪽을 손보며 입력 안정성을 다졌습니다.
- Python에서는 MongoDB 연동 구조를 정비해 분석 결과를 장기적으로 저장·활용할 기반을 강화했습니다.

### 2026-02-16
- Python 서버에 MongoDB 연동을 붙이고, 분석 총평과 액션 제안 필드를 확장했습니다.
- Spring은 FastAPI 연동 필드명과 CORS 흐름을 정리해 FE-BE-AI 연결 안정성을 높였습니다.
- 프론트는 대시보드와 상권/손님 분석 화면을 실제 API와 연결하는 방향으로 크게 전진했습니다.

### 2026-03-04
- 프론트의 JSX 전환과 리뷰 관리 화면 스크롤 동작을 정리했습니다.
- 실제 사용 흐름에서 눈에 띄던 UI 사용성 이슈를 조금씩 걷어냈습니다.

### 2026-03-06
- 상권 분석, 홍보 영상, 손님 분석 관련 화면과 연결 로직을 계속 손봤습니다.
- 데이터 기반 기능과 화면 간 연결이 점차 제품 형태를 갖추기 시작했습니다.

### 2026-03-09
- 루트 저장소에 서브모듈 자동화와 Git 운영을 돕는 스킬을 추가했습니다.
- 문서 작업과 개발 작업이 섞여도 흐름이 끊기지 않도록 운영 기반을 다졌습니다.

### 2026-03-11
- 사업계획서 관련 문서 자산과 내보내기 흐름을 정리했습니다.
- 프론트에서는 인플루언서 매칭 UI를 다듬고 툴팁 오류를 수정했습니다.

### 2026-03-17
- 루트 에이전트 가이드와 예창패 작업용 자산을 정리했습니다.
- 개발과 문서 작업을 함께 진행할 수 있도록 루트 차원의 작업 규칙과 컨텍스트를 보강했습니다.

### 2026-03-18
- 제안서 자산과 사업계획서 구조를 정리하고, 제출용 문서 흐름을 재정비했습니다.
- 동시에 프론트의 리뷰/인플루언서 관련 화면도 이어서 손보며 제품과 문서를 함께 밀어 올렸습니다.

### 2026-03-19
- 프론트에서 인증 흐름을 복구하고, 리뷰 관리 화면을 실데이터 기반으로 다시 연결했습니다.
- Spring에는 라이브 리뷰 관리용 API 연동을 추가해 템플릿 CRUD와 실제 데이터 흐름을 받을 수 있게 했습니다.
- Python 서버에서는 실제 리뷰 크롤링과 AI 리뷰 답변 생성 흐름을 강화했습니다.
- 이 시점부터 PULSE는 단순 목업이 아니라, 실제 리뷰를 읽고 답변을 생성하는 서비스다운 흐름에 가까워졌습니다.

### 2026-03-22
- 예창패 제출 패키지와 관련 문서를 마무리 단계까지 정리했습니다.
- 루트 저장소는 개발 기록과 문서 산출물을 함께 관리하는 형태로 한층 안정화되었습니다.

### 2026-03-25
- FE, Spring, Python 세 서버를 한 번에 점검하고 실행할 수 있도록 환경을 다시 정리했습니다.
- Python 가상환경, 프론트 의존성, 환경 변수 파일을 손봐 로컬 실행 흐름을 안정화했습니다.
- OpenAI 모델 설정을 최신 흐름에 맞게 조정하고, GPT-5 계열에서 막히던 호출 파라미터 이슈를 정리했습니다.
- MySQL 기준으로 실제 회원가입, 분석 실행, 대시보드 진입, 리뷰 관리, AI 답변 생성까지 E2E 테스트를 완료했습니다.
- 차트 영역에서는 Recharts 크기 warning이 반복되던 문제를 수정해 화면 안정성을 높였습니다.
- 리뷰 크롤링에서는 매장 검색 정규화와 후보 점수화 로직을 보강해 `스타벅스 강남R점` 조회 시 `압구정R점` 리뷰가 섞이던 문제를 해결했습니다.
- MongoDB 스냅샷 버전을 올려 새 크롤링 결과가 정확히 반영되도록 정리했습니다.
- 마지막으로 헤더의 목업 문구를 제거하고, 실제 로그인한 사용자 이름과 매장명이 표시되도록 연결했습니다.

### 2026-04-07
- 메인 저장소와 세 서브모듈을 최신 상태로 동기화하고, 프론트/스프링/파이썬 테스트가 한 번에 돌 수 있도록 전체 검증 흐름을 정리했습니다.
- 프론트 lint 설정과 스프링 테스트 깨짐을 복구하고, Python pytest 기반을 정비해 로컬 기본 테스트가 실패 없이 돌도록 맞췄습니다.
- 루트 `scripts/run_full_validation.ps1`와 프론트 `scripts/playwright-smoke.mjs`를 추가해 설치 후 바로 실행 가능한 통합 검증 경로를 만들었습니다.
- 실제 회원가입과 로그인, 대시보드 진입, 리뷰 관리 로딩, 손님 분석 확인까지 브라우저에서 직접 검증했습니다.
- `pulse_video` 레포의 홍보영상 생성 기능을 `pulse_python`에 통합하고, Veo 3.1 계열 모델과 DSPy 기반 프롬프트 최적화 흐름을 붙였습니다.

### 2026-04-08
- `홍보 영상 만들기` 탭의 타겟 손님 카드 하드코딩을 제거하고, `손님 분석`에서 생성된 실제 페르소나를 그대로 넘겨받도록 리팩터링했습니다.
- `AI 자동 완성`은 선택한 페르소나, 스타일 갤러리, 화질, 업로드 이미지에 따라 매번 다른 추천 프롬프트를 생성하도록 연결했습니다.
- `손님 분석 -> 맞춤 릴스 제작` 이동 시 선택한 페르소나가 그대로 유지되도록 네비게이션 컨텍스트를 정리했습니다.
- 프롬프트 추천은 빠른 응답용 경량 엔드포인트로 분리했고, 실제 영상 생성은 Veo quota/billing 상태와 분리해서 검증할 수 있도록 만들었습니다.
- 실제 MP4 생성은 현재 외부 영상 API quota/billing 상태에 영향을 받으며, 코드 레벨 연동과 프롬프트 추천 흐름 자체는 정상 검증 완료했습니다.

## 빠른 로컬 검증

다른 컴퓨터에서도 서브모듈까지 pull 받은 뒤 환경 변수와 의존성만 맞추면 바로 검증할 수 있도록 현재 기준을 정리합니다.

1. 저장소를 서브모듈 포함으로 받습니다.
   - `git clone --recurse-submodules <repo-url>`
   - 이미 clone했다면 `git submodule update --init --recursive`
2. 프론트 의존성을 설치합니다.
   - `cd pulse_FE`
   - `npm install`
3. Python 가상환경을 만들고 의존성을 설치합니다.
   - `cd pulse_python`
   - `python -m venv .venv`
   - `.venv\Scripts\activate`
   - `pip install -r requirements.txt`
4. Python 환경 변수를 준비합니다.
   - `pulse_python/.env.example`를 복사해 `.env.local` 또는 `.env`로 만듭니다.
   - 홍보영상 기능까지 테스트하려면 `OPENAI_API_KEY`, `GEMINI_API_KEY`, `VEO_API_KEY`, `GOOGLE_CLOUD_PROJECT`, `GOOGLE_CLOUD_LOCATION`, `VERTEX_VIDEO_API_KEY`를 채워야 합니다.
5. Spring은 별도 전역 설치 없이 wrapper로 실행할 수 있습니다.
   - `cd pulse_spring`
   - `.\gradlew.bat test`
6. 루트에서 통합 검증을 실행합니다.
   - `powershell -ExecutionPolicy Bypass -File .\scripts\run_full_validation.ps1`

참고:
- Playwright 스모크는 로컬 Chrome 또는 Edge가 설치되어 있으면 바로 실행됩니다. 필요하면 `PULSE_PLAYWRIGHT_BROWSER_PATH`로 브라우저 경로를 지정할 수 있습니다.
- 영상 생성 API 연동과 프롬프트 추천은 테스트 가능하지만, 실제 MP4 생성 성공 여부는 Veo quota/billing 상태에 따라 달라질 수 있습니다.

## 지금 우리가 만든 흐름

1. 사용자가 회원가입을 진행합니다.
2. Spring이 사용자/매장 정보를 저장하고 분석 작업을 요청합니다.
3. Python AI 서버가 리뷰를 수집하고 분석해 MongoDB에 결과와 스냅샷을 저장합니다.
4. 프론트는 저장된 결과를 받아 대시보드, 손님 분석, 리뷰 관리 화면에 실데이터를 표시합니다.
5. 홍보 영상 만들기에서는 손님 분석에서 도출된 실제 페르소나를 타겟 손님으로 넘겨 맞춤형 프롬프트와 영상 생성을 이어갈 수 있습니다.
6. 사용자는 분석 결과를 바탕으로 리뷰 답변과 실행 아이디어를 바로 확인할 수 있습니다.

## 다음에 보면 좋은 지점

- 마이페이지 안쪽에 남아 있는 일부 정적 문구를 실제 사용자 데이터와 더 자연스럽게 연결하기
- 리뷰 매칭 로직을 더 다양한 브랜드/지점명 케이스에서 검증하기
- 운영형 SaaS답게 반복 사용 지표와 유료 전환 흐름을 제품/문서 양쪽에서 함께 정교화하기

## 메모

이 README는 루트 저장소의 커밋 흐름과 현재 코드 상태, 그리고 최근 로컬 통합 작업 내용을 바탕으로 정리한 개발 일지입니다.  
문서 작업과 기능 개발이 함께 진행된 프로젝트 특성상, 앞으로도 이 파일은 "무엇을 만들었는지"와 "어디까지 정리됐는지"를 함께 기록하는 기준 문서로 사용할 수 있습니다.
